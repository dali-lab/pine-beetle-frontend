const MAP_INIT_CONSTANTS = {
  INIT_DELAY: 100,
  CONTAINER_CHECK_INTERVAL: 50,
  MAX_CONTAINER_CHECK_RETRIES: 100,
  STYLE_CHECK_INTERVAL: 1000,
  MAX_STYLE_CHECK_RETRIES: 10,
};

export default MAP_INIT_CONSTANTS;
